@AGENTS.md

## Claude Code

<!-- agentkit:begin — generated, do not hand-edit; run `agentkit apply` to update -->
<!--
  This file exists only because Claude Code does not read AGENTS.md at runtime:
  "Claude Code reads `CLAUDE.md`, not `AGENTS.md`." The import above is the whole point
  of the file. Everything below is Claude-only mechanics that would be noise in AGENTS.md.

  Keep this file under 10 lines of real content. Every line is always-loaded ON TOP of
  AGENTS.md, in every session, forever.

  A symlink (`ln -s AGENTS.md CLAUDE.md`) is preferable when there is no Claude-only
  content at all — except on Windows, where symlinks need Administrator or Developer Mode.
-->
Hooks and permissions: `.claude/settings.json`. Skills reach `.agents/skills` by relative symlink.
Auto memory: see `autoMemory` in `.agents/compatibility.json` for the declared state and the reason.
<!-- agentkit:end -->
